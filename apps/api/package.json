{
  "name": "@helux/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "build": "tsc",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p e2e",
    "lint": "tsc --noEmit",
    "test:e2e": "vitest run --config vitest.e2e.config.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@helux/ai": "workspace:*",
    "@helux/genetics": "workspace:*",
    "@helux/health": "workspace:*",
    "@helux/types": "workspace:*",
    "@helux/workouts": "workspace:*",
    "@supabase/supabase-js": "^2.49.1",
    "fastify": "^5.7.2",
    "tsx": "^4.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "*",
    "vitest": "^3.2.6"
  }
}
